/**
 * Couleurs de la vitrine publique qui ne viennent pas de la palette « brand ».
 *
 * Elles sont accordées au visuel de hero de la boutique : le rose de marque
 * jurait avec lui sur les deux éléments qui attirent l'œil en premier, le prix
 * et le panier. Elles vivent ici pour rester identiques d'une page à l'autre —
 * boutique, fiche article, kiosque.
 */

/** Fond des pastilles de prix. */
export const PRICE_BG = "#eb2d02";

/** Ombre portée assortie au fond des prix. */
export const PRICE_SHADOW = "0 16px 34px rgba(235, 45, 2, 0.24)";

/** Fond du bouton panier dans l'en-tête. */
export const CART_BG = "#001ab2";

/** Variante foncée du panier, quand le mini-panier est ouvert. */
export const CART_BG_DARK = "#00147f";

/** Ombre portée assortie au bouton panier. */
export const CART_SHADOW = "0 14px 34px rgba(0, 26, 178, 0.28)";
